# 版本已锁定（精确版本，与 CI 一致；升级前先跑 tests/）
PyMuPDF==1.28.2
python-docx==1.2.0
openpyxl==3.1.5
httpx==0.28.1
PyYAML==6.0.3

# paper_check 查重模块
numpy==2.5.2
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
# 可选：接入句向量模型后自动启用语义改写检测
# sentence-transformers>=3.0
